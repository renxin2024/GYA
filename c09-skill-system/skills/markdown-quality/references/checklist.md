# Markdown checklist

- The file starts with YAML frontmatter delimited by `---`.
- The frontmatter contains `title` and `description`.
- The article contains exactly one level-one heading.
- The article ends with a `## 总结` section.
